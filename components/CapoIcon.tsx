/**
 * Каподастр, вид сбоку — классический пружинный (триггерный).
 *
 * Отдельным компонентом, потому что рисуется в двух совсем разных местах —
 * крупной плашкой над текстом разбора и мелким чипом в строке каталога. Пока
 * иконка была бы выписана в каждом из них своей копией, они бы разъехались при
 * первой же правке, и один и тот же факт выглядел бы на сайте по-разному.
 *
 * ── Почему части ПЕРЕКРЫВАЮТСЯ ──────────────────────────────────────────────
 *
 * Губки и рычаг начинаются ВНУТРИ круга-шарнира, рукоять — внутри нижней губки.
 * Одним цветом это даёт литую фигуру без швов. Версия, где детали стыковались
 * встык, разваливалась на отдельные палочки: между ними оставались просветы.
 *
 * ── Что держит читаемость на 12 пикселях ────────────────────────────────────
 *
 * Не толщина линий, а ЗАЗОР МЕЖДУ ГУБКАМИ. Без него фигура схлопывается в
 * кляксу с отростками, и никакая проработка деталей этого не спасает. Поэтому
 * зазор здесь — самый охраняемый размер: около 1.9 единиц у носа и 2.8 у
 * шарнира, то есть он ещё и сходится к носу, как у настоящего зажима.
 *
 * Отсюда же два других решения. Линии тонкие — толстые съедали бы тот самый
 * зазор. А фигура растянута почти на весь холст (поля ~2.5 вместо ~4): при
 * одном и том же размере на экране это просто больше пикселей на каждую
 * деталь. Уменьшать надо массу, а не фигуру.
 *
 * Губки — заливка с сужением, а не штрих: у штриха толщина постоянна, а у
 * настоящего капо кончики тоньше. Нижняя вдобавок задрана левым краем вверх.
 */
export function CapoIcon({ size = 18 }: { size?: number }) {
  return (
    <svg width={size} height={size} viewBox="0 0 24 24" fill="currentColor" aria-hidden>
      {/* Верхняя губка: ложится на струны, сужается к левому кончику. */}
      <path d="M17.6 4.2 L4 5.7 C2.5 5.85 2.5 7.25 4 7.4 L18 6.5 Z" />
      {/* Нижняя губка: задрана левым краем вверх и сужается к носу. */}
      <path d="M17.5 9.5 L8.2 8.9 C6.9 8.8 6.8 10.3 8.1 10.4 L17.9 12.1 Z" />
      {/* Ось-шарнир: узел, в котором сходится вся конструкция. */}
      <circle cx="17.9" cy="7.9" r="2.4" />
      {/* Рычаг: от оси вниз-вправо, на него давят большим пальцем. */}
      <path
        d="M18.7 8.8 L20.2 17.8"
        stroke="currentColor"
        strokeWidth="1.8"
        strokeLinecap="round"
        fill="none"
      />
      {/* Пружинная рукоять: изгиб вниз-влево, «пистолетный» профиль. */}
      <path
        d="M9 10.5 C7.6 13.4 6.7 16.2 6.4 19"
        stroke="currentColor"
        strokeWidth="1.8"
        strokeLinecap="round"
        fill="none"
      />
    </svg>
  );
}
