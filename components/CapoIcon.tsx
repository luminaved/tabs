/**
 * Каподастр, вид сбоку — классический пружинный (триггерный).
 *
 * Отдельным компонентом, потому что рисуется в двух совсем разных местах —
 * крупной плашкой над текстом разбора и мелким чипом в строке каталога. Пока
 * иконка была бы выписана в каждом из них своей копией, они бы разъехались при
 * первой же правке, и один и тот же факт выглядел бы на сайте по-разному.
 *
 * ── Почему части ПЕРЕКРЫВАЮТСЯ, а не стыкуются ──────────────────────────────
 *
 * Все пять деталей намеренно заходят друг за друга: губки и рычаг начинаются
 * ВНУТРИ круга-шарнира, рукоять — внутри нижней губки. Одним цветом это даёт
 * литую фигуру без швов. Версия со стыками встык разваливалась на отдельные
 * палочки — между ними оставались просветы.
 *
 * ── Почему тонко ────────────────────────────────────────────────────────────
 *
 * Толстые штрихи и крупный шарнир съедали просвет между губками, а именно он
 * и делает фигуру каподастром: без зазора это просто клякса с отростками.
 * Поэтому массы ровно столько, чтобы деталь читалась, и ни единицей больше —
 * узнавание держится на ПРОМЕЖУТКАХ, а не на весе линий.
 *
 * Губки — заливка с сужением, а не штрих: у штриха толщина постоянна, а у
 * настоящего капо кончики тоньше. Нижняя вдобавок задрана левым краем вверх,
 * так что зазор между губками сходится к носу зажима.
 */
export function CapoIcon({ size = 18 }: { size?: number }) {
  return (
    <svg width={size} height={size} viewBox="0 0 24 24" fill="currentColor" aria-hidden>
      {/* Верхняя губка: ложится на струны, сужается к левому кончику. */}
      <path d="M17.6 5 L4.5 6.4 C3 6.55 3 7.95 4.5 8.1 L18 7.3 Z" />
      {/* Нижняя губка: задрана левым краем вверх и сужается к носу — зазор
          между губками сходится именно там, где зажим смыкается. */}
      <path d="M17.2 8.2 L8.6 8 C7.4 7.9 7.3 9.3 8.5 9.4 L17.8 10.9 Z" />
      {/* Ось-шарнир: узел, в котором сходится вся конструкция. Обе губки и
          рычаг начинаются внутри него — отсюда и цельность силуэта. */}
      <circle cx="17.8" cy="7.2" r="2.4" />
      {/* Рычаг: от оси вниз-вправо, на него давят большим пальцем. */}
      <path
        d="M18.6 8.6 L20.1 17.4"
        stroke="currentColor"
        strokeWidth="1.9"
        strokeLinecap="round"
        fill="none"
      />
      {/* Пружинная рукоять: изгиб вниз-влево, «пистолетный» профиль. */}
      <path
        d="M9.4 9.6 C7.9 12.6 7 15.6 6.6 18.6"
        stroke="currentColor"
        strokeWidth="1.9"
        strokeLinecap="round"
        fill="none"
      />
    </svg>
  );
}
